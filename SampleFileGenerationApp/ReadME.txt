Notes:
Program loads words dictionary that would be used to generate entries and generate random entries.

Things that could be improved:
- reading arguments is not fully implemented (due to time constraints) 
- Configuration file can be added (instead of passing values)